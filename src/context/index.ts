/**
 * Barrel file for context exports
 * 
 * Example usage:
 * import { useAuth, AuthProvider, useOnboarding, OnboardingProvider } from '@/context';
 */

// Export context below
export { useAuth, AuthProvider } from './AuthContext';
export { useOnboarding, OnboardingProvider } from './OnboardingContext'; 